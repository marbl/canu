# -*- makefile -*-

LIBUTL/     :=$(call MakePath,$/../../libutil/)
LIBBIO/     :=$(call MakePath,$/../../libbio/)

$/.CXX_SRCS := $/decodeMatch.C $/readHeader.C $/matchList.C $/featureList.C
$/.CXX_LIBS := $/libatac.a

$/.CLEAN :=$/*.o $/*~ $/core

$/libatac.a: ${$/.CXX_SRCS:.C=.o}

$(eval $/%.d $/%.o:   CXXFLAGS+=-I${LIBBIO/} -I${LIBUTL/})


