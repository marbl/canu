PROG    = test
INCLUDE = -I../libbri
LIBS    = -L../libbri -lbri

OBJS     = existDB.o

all: $(PROG)

test: test.o $(OBJS)
	$(CC) $(OPTS) $(INCLUDE) -o test test.o $(OBJS) $(LIBS) $(EFENCE)

clean:
	rm -f *.o $(PROG)

include ../Makefile-configuration
