# -*- makefile -*-

LIBUTL/     :=$(call MakePath,$/../libutil/)
LIBBIO/     :=$(call MakePath,$/../libbio/)
LIBMERYL/   :=$(call MakePath,$/../libmeryl/)
LIBKMER/    :=$(call MakePath,$/../libkmer/)

src      := $/trie.C

$/.CXX_SRCS := $(filter %.C,${src})
$/.CXX_EXES := $/trie

$/.CLEAN     :=$/*.o

$(eval $/%.d $/%.o:  CXXFLAGS+= -I${LIBUTL/} -I${LIBBIO/} -I${LIBMERYL/} -I${LIBKMER/})

$/trie:       ${$/.CXX_SRCS:.C=.o} \
              ${LIBBIO/}libbio.a \
              ${LIBUTL/}libutil.a
